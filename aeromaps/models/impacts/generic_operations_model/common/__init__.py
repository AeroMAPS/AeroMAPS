"""
common

========
Common package for the generic operations model implementation: hosts the factory
that instantiates per-concept gain models from user-defined configuration and the
model that composes the per-concept gains into the aggregate operational effects.
"""
