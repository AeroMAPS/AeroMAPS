"""
common

========
Common package for the generic offsets model implementation: hosts the factory that
instantiates the model from user-defined configuration and the model that computes
the per-scheme offset quantities and expenses and sums them into the aggregates.
"""
