data:
  outputs:
    json_outputs_file: "./outputs.json"

models:
  climate: false

  fleet: false

  energy:
    energy_carriers_model_data_file: "./energy_carriers_data.yaml"
    resources_model_data_file: "./resources_data.yaml"

  # Models are currently defined in the notebook.
  # In a future version, they will be loaded from this config.
  # standards:
  #   - models_optim_simple
  # customs:
  #   blend_completeness_constraint: "./custom_constraints.py::BlendCompletenessConstraint"
  #   biomass_availability_constraint_trajectory: "./custom_constraints.py::BiomassAvailabilityConstraintTrajectory"
  #   grid_electricity_availability_constraint_trajectory: "./custom_constraints.py::GridElectricityAvailabilityConstraintTrajectory"
